# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window:
# mod+shift+q
bind $mod+Shift+24 kill

# start dmenu (a program launcher)
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# mod+d
bind $mod+40 exec --no-startup-id i3-dmenu-desktop

# change focus
# mod+j, mod+k, mod+l, mod+ö
bind $mod+44 focus left
bind $mod+45 focus down
bind $mod+46 focus up
bind $mod+47 focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bind $mod+Shift+44 move left
bind $mod+Shift+45 move down
bind $mod+Shift+46 move up
bind $mod+Shift+47 move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation:
# mod+h
bind $mod+43 split h

# split in vertical orientation:
# mod+v
bind $mod+55 split v

# enter fullscreen mode for the focused container:
# mod+f
bind $mod+41 fullscreen

# change container layout (stacked, tabbed, toggle split)
# mod+s
bind $mod+39 layout stacking
# mod+w
bind $mod+25 layout tabbed
# mod+e
bind $mod+26 layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
# mod+a
bind $mod+38 focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bind $mod+10 workspace number 1
bind $mod+11 workspace number 2
bind $mod+12 workspace number 3
bind $mod+13 workspace number 4
bind $mod+14 workspace number 5
bind $mod+15 workspace number 6
bind $mod+16 workspace number 7
bind $mod+17 workspace number 8
bind $mod+18 workspace number 9
bind $mod+19 workspace number 10
bind $mod+20 workspace number 11
bind $mod+21 workspace number 12

# move focused container to workspace
bind $mod+Shift+10 move container to workspace number 1
bind $mod+Shift+11 move container to workspace number 2
bind $mod+Shift+12 move container to workspace number 3
bind $mod+Shift+13 move container to workspace number 4
bind $mod+Shift+14 move container to workspace number 5
bind $mod+Shift+15 move container to workspace number 6
bind $mod+Shift+16 move container to workspace number 7
bind $mod+Shift+17 move container to workspace number 8
bind $mod+Shift+18 move container to workspace number 9
bind $mod+Shift+19 move container to workspace number 10
bind $mod+Shift+20 move container to workspace number 11
bind $mod+Shift+21 move container to workspace number 12

# reload the configuration file
# mod+shift+C
bind $mod+Shift+54 reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
# mod+Shift+R
bind $mod+Shift+27 restart
# exit i3 (logs you out of your X session)
# mod+Shift+E
bind $mod+Shift+26 exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bind 44 resize shrink width 10 px or 10 ppt
        bind 45 resize grow height 10 px or 10 ppt
        bind 46 resize shrink height 10 px or 10 ppt
        bind 47 resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

# resize window
# mod+r
bind $mod+27 mode "resize"

# assign floating
#for_window [class="LoLPatcher.exe"] floating enable

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3status
}

new_window 1pixel

#bind $mod+96 exec "i3lock -t --image=/home/sporz/custom/lock"
bind $mod+96 exec "i3lock -c `cat /dev/urandom | tr -dc 'a-f0-9' | head -c 6`"

assign [class="Nautilus"] 9

exec unity-settings-daemon
exec gnome-terminal
#exec firefox
exec sleep 5; google-chrome
exec pa-applet
exec sleep 5; feh --bg-max /home/sporz/custom/bgimage
#exec /home/sporz/set_xrandr.sh

